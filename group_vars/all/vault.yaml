# Encrypted secrets - run `ansible-vault encrypt group_vars/all/vault.yaml` after editing
# Or use `make vault-edit` to edit in place
---

# SSL certificate email for Let's Encrypt notifications
# vault_ssl_cert_email: your-email@example.com

# Future: Database credentials, API keys, etc.
# vault_database_url: postgresql://user:pass@host/db
